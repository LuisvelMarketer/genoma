---
summary: "CLI reference for `genoma clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `genoma clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `genoma clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `genoma clawbot qr` (same behavior as [`genoma qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `genoma clawbot qr` -> `genoma qr`
