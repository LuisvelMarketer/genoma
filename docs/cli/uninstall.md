---
summary: "CLI reference for `genoma uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `genoma uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
genoma uninstall
genoma uninstall --all --yes
genoma uninstall --dry-run
```
